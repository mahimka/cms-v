<%= erb :'/layout/_flash', views: settings.views %>

<div class="content m-3">

  <h1 class="title">New event</h1>
  <p class="subtitle is-6">Step 1: name and schema. Details, tags, links, pictures and dates can be filled in on the next step.</p>

  <form method="POST" action="/admin/events">

    <div class="columns">

      <div class="column is-2">
        <%= label(:event, :name, "name", options = {class: 'label'}) %>
      </div>

      <div class="column is-9">
        <%= input(:event, :name, options = {class: 'input is-success', value: @event.name, placeholder: "enter event name"}) %>
      </div>

    </div>

    <div class="columns">

      <div class="column is-2">
        <%= label(:event, :schema_id, "schema", options = {class: 'label'}) %>
      </div>

      <div class="column is-9">
        <% @schemas_for_select = {"": ""}.merge(Schema.active.order(:ancestry, :position, :name).to_h { |schema| [schema.id, ("— " * schema.depth) + schema.name] }) %>
        <%= select(:event, :schema_id, @schemas_for_select, options = {class: 'select input is-success', value: @event.schema_id, required: true}) %>
      </div>

    </div>

    <div class="field is-grouped">
      <div class="control">
        <input type="submit" value="Create Event" class="button is-warning">
      </div>
    </div>

  </form>

</div>
