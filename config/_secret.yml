cms:
  login: max
  password: 123

# Gemini API key: https://aistudio.google.com/apikey
gemini_api_key:

# Постоянный ключ для подписи сессионных cookie — сгенерировать, например,
# `ruby -rsecurerandom -e 'puts SecureRandom.hex(64)'`
session_secret: